import React, { type PropsWithChildren, memo } from 'react'


export default memo(function HeadMenuWrapper(props: PropsWithChildren<{}>) {

  const render_dr_menu = (props: {}) => {}

  return(
    <>
      菜单区域
    </>
  )
})