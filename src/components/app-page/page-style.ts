import styled from 'styled-components'

const PageCenterwrapper = styled.main`
  width: 1200px;
  background-color: #fff;
  margin: 0 auto;
  min-height: calc(100vh - 100px);
  padding: 10px 0;
`

const PageContWrapper = styled.main`
  width: 100vw;
  background-color: #fff;
  margin: 0 auto;
`

export { PageCenterwrapper, PageContWrapper }